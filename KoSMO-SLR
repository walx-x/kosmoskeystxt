--============================================================
-- KOSMOS HUB V4 — FULL UNRESTRICTED EXTENDED MASTER EDITION
-- Complete Framework with Combat, Visuals, Movement, Weapon Modifiers,
-- Player Utilities, Advanced Configuration Manager & Full UI Elements
--============================================================

--// SERVICES
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local VirtualUser = game:GetService("VirtualUser")
local StarterGui = game:GetService("StarterGui")

local LocalPlayer = Players.LocalPlayer

--============================================================
-- SETTINGS & GLOBAL STATE
--============================================================

local Settings = {
    -- Silent Aim & Combat Engine
    Enabled = false,
    ShowFOV = false,
    ShowDot = false,
    BaseFOV = 60,
    DynamicFOV = false,
    WallCheck = false,
    TeamCheck = false,
    HitPatternEnabled = false,
    TargetBone = "Head",
    HitProbability = 100,
    FOVColor = Color3.fromRGB(145, 45, 255),
    RainbowFOV = false,
    TargetColor = Color3.fromRGB(255, 45, 85),
    ToggleKey = Enum.KeyCode.RightControl,
    UnloadKey = Enum.KeyCode.End,
    
    -- Visuals & ESP Engine
    ESPEnabled = false,
    ESPBoxes = false,
    ESPNames = false,
    ESPDistance = false,
    ESPHealth = false,
    ESPChams = false,
    ESPTracers = false,
    Fullbright = false,
    Crosshair = false,
    ChamColor = Color3.fromRGB(145, 45, 255),
    ESPColor = Color3.fromRGB(255, 255, 255),
    TracerColor = Color3.fromRGB(145, 45, 255),

    -- Movement Engine
    EnableWalkSpeed = false,
    WalkSpeed = 16,
    EnableJumpPower = false,
    JumpPower = 50,
    InfiniteJump = false,
    Noclip = false,
    BHop = false,
    FlyEnabled = false,
    FlySpeed = 50,

    -- Weapon & Gun Modifiers (Universal Hooks & Frameworks)
    NoRecoil = false,
    NoSpread = false,
    RapidFire = false,
    InfiniteAmmo = false,
    InstantReload = false,
    AlwaysAuto = false,

    -- Player & Anti-Aim Engine
    Spinbot = false,
    SpinSpeed = 30,
    AntiStun = false,
    GodModeUniversal = false,

    -- Misc & Utility Engine
    AntiAFK = false,
    FPSUnlocker = false,
    LowGraphics = false,

    -- UI Theme Customization
    Accent = Color3.fromRGB(145, 45, 255),
    Accent2 = Color3.fromRGB(205, 80, 255),
    Background = Color3.fromRGB(10, 8, 15),
    Sidebar = Color3.fromRGB(15, 12, 22),
    Panel = Color3.fromRGB(19, 16, 27),
    Element = Color3.fromRGB(27, 23, 37),
    Border = Color3.fromRGB(58, 45, 76),
    Text = Color3.fromRGB(245, 242, 250),
    SubText = Color3.fromRGB(150, 143, 163),
    Green = Color3.fromRGB(80, 220, 135),
    Red = Color3.fromRGB(240, 75, 100),
    UIScale = 1
}

local Values = {}
local hitPattern = {"Head", "Torso", "Torso", "Torso", "LeftLeg", "LeftLeg", "RightLeg"}
local currentPatternIndex = 1
local renderConnection, steppedConnection, jumpConnection, inputConnection, afkConnection, weaponLoopConnection
local oldFire, SimpleCastModule
local espFolder, tracerFolder
local origAmbient, origBrightness, origShadows = Lighting.Ambient, Lighting.Brightness, Lighting.GlobalShadows
local newConfigInputName = "default"
local selectedDropdownConfig = ""

--============================================================
-- CONFIGURATION FILE SYSTEM (FULL READ/WRITE ENGINE)
--============================================================

local CONFIG_FOLDER = "KosmosHub_Configs"
local AUTOLOAD_FILE = CONFIG_FOLDER .. "/autoload.txt"

local function EnsureFolder()
    if makefolder and isfolder and not isfolder(CONFIG_FOLDER) then
        makefolder(CONFIG_FOLDER)
    end
end

local function GetSavedConfigs()
    EnsureFolder()
    local configs = {}
    if listfiles then
        local success, files = pcall(function() return listfiles(CONFIG_FOLDER) end)
        if success and files then
            for _, filePath in ipairs(files) do
                if filePath:sub(-5) == ".json" then
                    local fileName = filePath:match("([^/^\\]+)%.json$")
                    if fileName then table.insert(configs, fileName) end
                end
            end
        end
    end
    return configs
end

local function SaveConfig(name)
    EnsureFolder()
    if not writefile or name == "" then return false end
    local configData = {}
    for k, v in pairs(Settings) do
        if typeof(v) == "Color3" then
            configData[k] = {R = v.R, G = v.G, B = v.B, Type = "Color3"}
        elseif typeof(v) == "EnumItem" then
            configData[k] = {Name = v.Name, EnumType = tostring(v.EnumType), Type = "Enum"}
        else
            configData[k] = v
        end
    end
    return pcall(function()
        writefile(CONFIG_FOLDER .. "/" .. name .. ".json", HttpService:JSONEncode(configData))
    end)
end

local function LoadConfig(name)
    EnsureFolder()
    if not readfile or not isfile then return false end
    local filePath = CONFIG_FOLDER .. "/" .. name .. ".json"
    if not isfile(filePath) then return false end
    local success, result = pcall(function() return HttpService:JSONDecode(readfile(filePath)) end)
    if success and type(result) == "table" then
        for k, v in pairs(result) do
            if Settings[k] ~= nil then
                if type(v) == "table" and v.Type == "Color3" then
                    Settings[k] = Color3.new(v.R, v.G, v.B)
                elseif type(v) == "table" and v.Type == "Enum" then
                    pcall(function() Settings[k] = Enum[v.EnumType][v.Name] end)
                else
                    Settings[k] = v
                end
            end
        end
        return true
    end
    return false
end

local function DeleteConfig(name)
    EnsureFolder()
    if not delfile or not isfile then return false end
    local filePath = CONFIG_FOLDER .. "/" .. name .. ".json"
    if isfile(filePath) then return pcall(function() delfile(filePath) end) end
    return false
end

local function SetAutoLoadConfig(name)
    EnsureFolder()
    if writefile then writefile(AUTOLOAD_FILE, name or "") end
end

local function GetAutoLoadConfig()
    EnsureFolder()
    if isfile and readfile and isfile(AUTOLOAD_FILE) then
        local content = readfile(AUTOLOAD_FILE):gsub("%s+", "")
        if content ~= "" then return content end
    end
    return nil
end

--============================================================
-- INSTANCE CLEANUP & CONTAINERS
--============================================================

local GUI_NAME = "KOSMOS_HUB_V4_MASTER"
if CoreGui:FindFirstChild(GUI_NAME) then CoreGui[GUI_NAME]:Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = GUI_NAME
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = CoreGui

espFolder = Instance.new("Folder")
espFolder.Name = "Kosmos_ESP_Holder"
espFolder.Parent = CoreGui

tracerFolder = Instance.new("Folder")
tracerFolder.Name = "Kosmos_Tracer_Holder"
tracerFolder.Parent = ScreenGui

--============================================================
-- INTERFACE CONSTRUCTOR HELPERS
--============================================================

local function Create(class, props, parent)
    local obj = Instance.new(class)
    for property, value in pairs(props or {}) do obj[property] = value end
    obj.Parent = parent
    return obj
end

local function Corner(obj, radius)
    return Create("UICorner", { CornerRadius = UDim.new(0, radius) }, obj)
end

local function Stroke(obj, color, thickness, transparency)
    return Create("UIStroke", {
        Color = color or Settings.Border,
        Thickness = thickness or 1,
        Transparency = transparency or 0
    }, obj)
end

local function Tween(obj, info, properties)
    return TweenService:Create(obj, info, properties)
end

local function Label(parent, text, size, color, font)
    return Create("TextLabel", {
        BackgroundTransparency = 1,
        Text = text,
        TextColor3 = color or Settings.Text,
        TextSize = size or 11,
        Font = font or Enum.Font.GothamMedium,
        TextXAlignment = Enum.TextXAlignment.Left,
        TextYAlignment = Enum.TextYAlignment.Center
    }, parent)
end

local function getCamera()
    return workspace.CurrentCamera or workspace:FindFirstChildOfClass("Camera")
end

local function getMousePosition()
    local pos = UserInputService:GetMouseLocation()
    return Vector2.new(pos.X, pos.Y)
end

--============================================================
-- HUD & FOV OVERLAY COMPONENTS
--============================================================

local FOVFrame = Create("Frame", {
    Name = "FOVFrame",
    BackgroundTransparency = 1,
    Size = UDim2.new(0, Settings.BaseFOV * 2, 0, Settings.BaseFOV * 2),
    AnchorPoint = Vector2.new(0.5, 0.5),
    Visible = false
}, ScreenGui)

local FOVStroke = Stroke(FOVFrame, Settings.FOVColor, 1.5, 0.3)
Corner(FOVFrame, 999)

local TargetDotFrame = Create("Frame", {
    Name = "TargetDot",
    Size = UDim2.new(0, 8, 0, 8),
    AnchorPoint = Vector2.new(0.5, 0.5),
    BackgroundColor3 = Settings.TargetColor,
    BorderSizePixel = 0,
    Visible = false,
    ZIndex = 10
}, ScreenGui)

Stroke(TargetDotFrame, Color3.fromRGB(0, 0, 0), 1.5, 0)
Corner(TargetDotFrame, 999)

local CrosshairFrame = Create("Frame", {
    Name = "CrosshairCenter",
    Position = UDim2.new(0.5, 0, 0.5, 0),
    Size = UDim2.new(0, 1, 0, 1),
    BackgroundTransparency = 1,
    Visible = false,
    ZIndex = 20
}, ScreenGui)

Create("Frame", { Size = UDim2.new(0, 14, 0, 2), Position = UDim2.new(0, -7, 0, -1), BackgroundColor3 = Settings.Accent, BorderSizePixel = 0 }, CrosshairFrame)
Create("Frame", { Size = UDim2.new(0, 2, 0, 14), Position = UDim2.new(0, -1, 0, -7), BackgroundColor3 = Settings.Accent, BorderSizePixel = 0 }, CrosshairFrame)

--============================================================
-- COMBAT & TARGETING CALCULATIONS
--============================================================

local function getTargetPart(character, targetName)
    if not character then return nil end
    if targetName == "Torso" then
        return character:FindFirstChild("Torso") or character:FindFirstChild("UpperTorso")
    elseif targetName == "LeftLeg" then
        return character:FindFirstChild("Left Leg") or character:FindFirstChild("LeftUpperLeg")
    elseif targetName == "RightLeg" then
        return character:FindFirstChild("Right Leg") or character:FindFirstChild("RightUpperLeg")
    else
        return character:FindFirstChild(targetName) or character:FindFirstChild("Head")
    end
end

local function isTargetVisible(part)
    if not Settings.WallCheck then return true end
    local cam = getCamera()
    if not cam then return false end
    local ignoreList = {LocalPlayer.Character, part.Parent}
    return #cam:GetPartsObscuringTarget({part.Position}, ignoreList) == 0
end

local function getCurrentFOV()
    if not Settings.DynamicFOV then return Settings.BaseFOV end
    local cam = getCamera()
    if not cam then return Settings.BaseFOV end

    local closestDist = math.huge
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and (not Settings.TeamCheck or player.Team ~= LocalPlayer.Team) then
            local root = player.Character:FindFirstChild("HumanoidRootPart")
            if root then
                local dist = (root.Position - cam.CFrame.Position).Magnitude
                if dist < closestDist then closestDist = dist end
            end
        end
    end
    if closestDist ~= math.huge and closestDist > 0 then
        return math.clamp(Settings.BaseFOV * (50 / closestDist), 12, 220)
    end
    return Settings.BaseFOV
end

local function getBestTarget(maxDistance)
    local cam = getCamera()
    if not cam then return nil, nil end

    local mousePos = getMousePosition()
    local bestDist, bestPart, bestScreenPos = maxDistance, nil, nil
    local targetName = Settings.HitPatternEnabled and hitPattern[currentPatternIndex] or Settings.TargetBone

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and (not Settings.TeamCheck or player.Team ~= LocalPlayer.Team) then
            local humanoid = player.Character:FindFirstChildWhichIsA("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local part = getTargetPart(player.Character, targetName)
                if part then
                    local screenPos, onScreen = cam:WorldToViewportPoint(part.Position)
                    if onScreen then
                        local screenVec = Vector2.new(screenPos.X, screenPos.Y)
                        local distToMouse = (screenVec - mousePos).Magnitude
                        if distToMouse < bestDist and isTargetVisible(part) then
                            bestDist = distToMouse
                            bestPart = part
                            bestScreenPos = screenVec
                        end
                    end
                end
            end
        end
    end
    return bestPart, bestScreenPos
end

--============================================================
-- VISUALS & ESP RENDERING SYSTEM
--============================================================

local function updateESP()
    espFolder:ClearAllChildren()
    tracerFolder:ClearAllChildren()
    if not Settings.ESPEnabled then return end

    local cam = getCamera()
    if not cam then return end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            if not Settings.TeamCheck or player.Team ~= LocalPlayer.Team then
                local char = player.Character
                local root = char:FindFirstChild("HumanoidRootPart")
                local humanoid = char:FindFirstChildWhichIsA("Humanoid")

                if root and humanoid and humanoid.Health > 0 then
                    local screenPos, onScreen = cam:WorldToViewportPoint(root.Position)

                    -- Snaplines / Tracers
                    if Settings.ESPTracers and onScreen then
                        local line = Instance.new("Frame")
                        line.BorderSizePixel = 0
                        line.BackgroundColor3 = Settings.TracerColor
                        line.AnchorPoint = Vector2.new(0.5, 0.5)

                        local startPos = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y)
                        local targetVec = Vector2.new(screenPos.X, screenPos.Y)
                        local distance = (targetVec - startPos).Magnitude
                        local angle = math.atan2(targetVec.Y - startPos.Y, targetVec.X - startPos.X)

                        line.Size = UDim2.new(0, distance, 0, 1.5)
                        line.Position = UDim2.new(0, (startPos.X + targetVec.X) / 2, 0, (startPos.Y + targetVec.Y) / 2)
                        line.Rotation = math.deg(angle)
                        line.Parent = tracerFolder
                    end

                    -- Player Chams Highlight
                    if Settings.ESPChams then
                        local highlight = Instance.new("Highlight")
                        highlight.Adornee = char
                        highlight.FillColor = Settings.ChamColor
                        highlight.FillTransparency = 0.5
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        highlight.OutlineTransparency = 0
                        highlight.Parent = espFolder
                    end

                    -- Billboard HUD Overlays
                    if Settings.ESPBoxes or Settings.ESPNames or Settings.ESPDistance or Settings.ESPHealth then
                        local bb = Instance.new("BillboardGui")
                        bb.Adornee = root
                        bb.Size = UDim2.new(4.2, 0, 5.6, 0)
                        bb.AlwaysOnTop = true
                        bb.Parent = espFolder

                        if Settings.ESPBoxes then
                            local box = Instance.new("Frame")
                            box.Size = UDim2.new(1, 0, 1, 0)
                            box.BackgroundTransparency = 1
                            box.Parent = bb
                            Stroke(box, Settings.ESPColor, 1.5, 0)
                        end

                        if Settings.ESPNames then
                            local nameLbl = Instance.new("TextLabel")
                            nameLbl.Size = UDim2.new(1, 0, 0, 14)
                            nameLbl.Position = UDim2.new(0, 0, 0, -18)
                            nameLbl.BackgroundTransparency = 1
                            nameLbl.Text = player.DisplayName .. " (@" .. player.Name .. ")"
                            nameLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
                            nameLbl.TextSize = 10
                            nameLbl.Font = Enum.Font.GothamBold
                            nameLbl.TextStrokeTransparency = 0.4
                            nameLbl.Parent = bb
                        end

                        if Settings.ESPDistance then
                            local dist = math.floor((root.Position - cam.CFrame.Position).Magnitude)
                            local distLbl = Instance.new("TextLabel")
                            distLbl.Size = UDim2.new(1, 0, 0, 14)
                            distLbl.Position = UDim2.new(0, 0, 1, 4)
                            distLbl.BackgroundTransparency = 1
                            distLbl.Text = "[" .. tostring(dist) .. "m]"
                            distLbl.TextColor3 = Settings.Accent2
                            distLbl.TextSize = 9
                            distLbl.Font = Enum.Font.GothamBold
                            distLbl.TextStrokeTransparency = 0.4
                            distLbl.Parent = bb
                        end

                        if Settings.ESPHealth then
                            local healthPct = math.clamp(humanoid.Health / humanoid.MaxHealth, 0, 1)
                            local healthBg = Instance.new("Frame")
                            healthBg.Size = UDim2.new(0, 4, 1, 0)
                            healthBg.Position = UDim2.new(0, -8, 0, 0)
                            healthBg.BackgroundColor3 = Color3.fromRGB(20, 18, 25)
                            healthBg.BorderSizePixel = 0
                            healthBg.Parent = bb
                            Corner(healthBg, 2)

                            local healthFill = Instance.new("Frame")
                            healthFill.Size = UDim2.new(1, 0, healthPct, 0)
                            healthFill.Position = UDim2.new(0, 0, 1 - healthPct, 0)
                            healthFill.BackgroundColor3 = Settings.Red:Lerp(Settings.Green, healthPct)
                            healthFill.BorderSizePixel = 0
                            healthFill.Parent = healthBg
                            Corner(healthFill, 2)
                        end
                    end
                end
            end
        end
    end
end

--============================================================
-- WEAPON MODIFIERS ENGINE
--============================================================

local function applyWeaponModifiers()
    if not LocalPlayer.Character then return end
    local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if not tool then return end

    -- Loop through Tool modules / attributes to override recoil & spread values
    for _, item in ipairs(tool:GetDescendants()) do
        if item:IsA("ModuleScript") then
            local success, content = pcall(function() return require(item) end)
            if success and type(content) == "table" then
                if Settings.NoRecoil then
                    if content.Recoil ~= nil then content.Recoil = 0 end
                    if content.recoil ~= nil then content.recoil = 0 end
                    if content.RecoilControl ~= nil then content.RecoilControl = 0 end
                end
                if Settings.NoSpread then
                    if content.Spread ~= nil then content.Spread = 0 end
                    if content.spread ~= nil then content.spread = 0 end
                    if content.Accuracy ~= nil then content.Accuracy = 1 end
                end
                if Settings.RapidFire then
                    if content.FireRate ~= nil then content.FireRate = 0.02 end
                    if content.fireRate ~= nil then content.fireRate = 0.02 end
                    if content.Cooldown ~= nil then content.Cooldown = 0.02 end
                end
                if Settings.AlwaysAuto then
                    if content.Auto ~= nil then content.Auto = true end
                    if content.Mode ~= nil then content.Mode = "Auto" end
                end
            end
        end
    end
end

--============================================================
-- RUNTIME CONNECTIONS & LOOPING
--============================================================

jumpConnection = UserInputService.JumpRequest:Connect(function()
    if Settings.InfiniteJump and LocalPlayer.Character then
        local humanoid = LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
        if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

steppedConnection = RunService.Stepped:Connect(function()
    if LocalPlayer.Character then
        local humanoid = LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
        local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

        if humanoid then
            if Settings.EnableWalkSpeed then humanoid.WalkSpeed = Settings.WalkSpeed end
            if Settings.EnableJumpPower then humanoid.UseJumpPower = true; humanoid.JumpPower = Settings.JumpPower end
            if Settings.BHop and humanoid.FloorMaterial ~= Enum.Material.Air and humanoid.MoveDirection.Magnitude > 0 then
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end

        -- CFrame Fly System
        if Settings.FlyEnabled and root then
            local cam = getCamera()
            local flyVec = Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then flyVec = flyVec + cam.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then flyVec = flyVec - cam.CFrame.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then flyVec = flyVec - cam.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then flyVec = flyVec + cam.CFrame.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then flyVec = flyVec + Vector3.yAxis end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then flyVec = flyVec - Vector3.yAxis end

            root.Velocity = Vector3.zero
            if flyVec.Magnitude > 0 then
                root.CFrame = root.CFrame + (flyVec.Unit * (Settings.FlySpeed / 10))
            end
        end

        -- Spinbot Anti-Aim
        if Settings.Spinbot and root then
            root.CFrame = root.CFrame * CFrame.Angles(0, math.rad(Settings.SpinSpeed), 0)
        end

        -- Noclip
        if Settings.Noclip or Settings.FlyEnabled then
            for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end
end)

renderConnection = RunService.RenderStepped:Connect(function()
    if Settings.RainbowFOV then Settings.FOVColor = Color3.fromHSV((tick() % 5) / 5, 0.8, 1) end
    if Settings.Fullbright then
        Lighting.Ambient = Color3.new(1,1,1)
        Lighting.Brightness = 2
        Lighting.GlobalShadows = false
    end

    CrosshairFrame.Visible = Settings.Crosshair
    local activeFOV = getCurrentFOV()
    local mousePos = getMousePosition()

    if Settings.Enabled and Settings.ShowFOV then
        FOVFrame.Visible = true
        FOVFrame.Size = UDim2.new(0, activeFOV * 2, 0, activeFOV * 2)
        FOVFrame.Position = UDim2.new(0, mousePos.X, 0, mousePos.Y)
        FOVStroke.Color = Settings.FOVColor
    else
        FOVFrame.Visible = false
    end

    local bestPart, bestScreenPos = getBestTarget(activeFOV)
    if Settings.Enabled and Settings.ShowDot and bestScreenPos then
        TargetDotFrame.Visible = true
        TargetDotFrame.Position = UDim2.new(0, bestScreenPos.X, 0, bestScreenPos.Y)
    else
        TargetDotFrame.Visible = false
    end

    applyWeaponModifiers()
    updateESP()
end)

-- Anti-AFK Handler
afkConnection = LocalPlayer.Idled:Connect(function()
    if Settings.AntiAFK then
        VirtualUser:Button2Down(Vector2.zero, workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.zero, workspace.CurrentCamera.CFrame)
    end
end)

-- Universal Gun Engine Hooking (SimpleCast)
pcall(function()
    SimpleCastModule = require(ReplicatedStorage:FindFirstChild("SimpleCast", true))
    if SimpleCastModule and SimpleCastModule.Fire then
        oldFire = SimpleCastModule.Fire
        SimpleCastModule.Fire = function(self, origin, direction, speed, options)
            if Settings.Enabled then
                local roll = math.random(1, 100)
                if roll <= Settings.HitProbability then
                    local activeFOV = getCurrentFOV()
                    local targetPart, _ = getBestTarget(activeFOV)
                    if targetPart then
                        direction = (targetPart.Position - origin).Unit
                        if Settings.HitPatternEnabled then
                            currentPatternIndex = (currentPatternIndex % #hitPattern) + 1
                        end
                    end
                end
            end
            return oldFire(self, origin, direction, speed, options)
        end
    end
end)

local function UnloadScript()
    if renderConnection then renderConnection:Disconnect() end
    if steppedConnection then steppedConnection:Disconnect() end
    if jumpConnection then jumpConnection:Disconnect() end
    if inputConnection then inputConnection:Disconnect() end
    if afkConnection then afkConnection:Disconnect() end
    if SimpleCastModule and oldFire then SimpleCastModule.Fire = oldFire end
    
    Lighting.Ambient = origAmbient
    Lighting.Brightness = origBrightness
    Lighting.GlobalShadows = origShadows

    if espFolder then espFolder:Destroy() end
    if tracerFolder then tracerFolder:Destroy() end
    if ScreenGui then ScreenGui:Destroy() end
end

--============================================================
-- ANIMATED INITIALIZATION LOADING SCREEN
--============================================================

local function ShowLoadingScreen(onComplete)
    local LoadingFrame = Create("Frame", {
        Name = "LoadingFrame",
        Size = UDim2.fromOffset(380, 220),
        Position = UDim2.fromScale(0.5, 0.5),
        AnchorPoint = Vector2.new(0.5, 0.5),
        BackgroundColor3 = Settings.Background,
        BorderSizePixel = 0,
        ZIndex = 100
    }, ScreenGui)

    Corner(LoadingFrame, 12)
    Stroke(LoadingFrame, Settings.Accent, 1.5)

    local Title = Label(LoadingFrame, "KOSMOS HUB V4", 22, Settings.Text, Enum.Font.GothamBlack)
    Title.Position = UDim2.fromOffset(0, 25)
    Title.Size = UDim2.new(1, 0, 0, 25)
    Title.TextXAlignment = Enum.TextXAlignment.Center

    local Subtitle = Label(LoadingFrame, "EXTENDED UNRESTRICTED EDITION", 9, Settings.Accent2, Enum.Font.GothamBold)
    Subtitle.Position = UDim2.fromOffset(0, 52)
    Subtitle.Size = UDim2.new(1, 0, 0, 16)
    Subtitle.TextXAlignment = Enum.TextXAlignment.Center

    local StatusLbl = Label(LoadingFrame, "Caricamento moduli universali in corso...", 10, Settings.SubText, Enum.Font.GothamMedium)
    StatusLbl.Position = UDim2.fromOffset(0, 105)
    StatusLbl.Size = UDim2.new(1, 0, 0, 20)
    StatusLbl.TextXAlignment = Enum.TextXAlignment.Center

    local BarBackground = Create("Frame", {
        Size = UDim2.new(0.8, 0, 0, 8),
        Position = UDim2.new(0.1, 0, 0, 140),
        BackgroundColor3 = Settings.Element,
        BorderSizePixel = 0
    }, LoadingFrame)
    Corner(BarBackground, 4)

    local BarFill = Create("Frame", {
        Size = UDim2.new(0, 0, 1, 0),
        BackgroundColor3 = Settings.Accent,
        BorderSizePixel = 0
    }, BarBackground)
    Corner(BarFill, 4)

    task.spawn(function()
        local steps = {
            {pct = 0.2, text = "Inizializzazione grafica & UI..."},
            {pct = 0.45, text = "Caricamento Silent Aim & FOV Engine..."},
            {pct = 0.70, text = "Configurazione CFrame Fly & Movement..."},
            {pct = 0.90, text = "Setup Weapon Modifiers & ESP..."},
            {pct = 1.00, text = "Inizializzazione completata!"}
        }

        for _, step in ipairs(steps) do
            Tween(BarFill, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = UDim2.new(step.pct, 0, 1, 0) }):Play()
            StatusLbl.Text = step.text
            task.wait(0.4)
        end

        task.wait(0.2)
        Tween(LoadingFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { BackgroundTransparency = 1 }):Play()
        task.wait(0.3)
        LoadingFrame:Destroy()

        if onComplete then onComplete() end
    end)
end

--============================================================
-- MAIN DASHBOARD INTERFACE
--============================================================

local Main = Create("Frame", {
    Name = "Main",
    Size = UDim2.fromOffset(1050, 680),
    Position = UDim2.fromScale(0.5, 0.5),
    AnchorPoint = Vector2.new(0.5, 0.5),
    BackgroundColor3 = Settings.Background,
    BorderSizePixel = 0,
    Visible = false,
    Active = true,
    ZIndex = 10
}, ScreenGui)

Corner(Main, 12)
Stroke(Main, Settings.Border, 1)

local MainScale = Create("UIScale", { Scale = Settings.UIScale }, Main)

-- Window Dragging Mechanics
do
    local dragging, dragStart, startPosition
    Main.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPosition = Main.Position
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            Main.Position = UDim2.new(
                startPosition.X.Scale, startPosition.X.Offset + delta.X,
                startPosition.Y.Scale, startPosition.Y.Offset + delta.Y
            )
        end
    end)
end

-- Sidebar Navigation Setup
local Sidebar = Create("Frame", { Size = UDim2.new(0, 220, 1, 0), BackgroundColor3 = Settings.Sidebar, BorderSizePixel = 0 }, Main)
Corner(Sidebar, 12)

local LogoFrame = Create("Frame", { Size = UDim2.fromOffset(190, 70), Position = UDim2.fromOffset(15, 10), BackgroundTransparency = 1 }, Sidebar)
local LogoGlow = Create("Frame", { Size = UDim2.fromOffset(42, 42), Position = UDim2.fromOffset(5, 13), BackgroundColor3 = Settings.Accent, BorderSizePixel = 0 }, LogoFrame)
Corner(LogoGlow, 12)
local LogoText = Label(LogoGlow, "K", 21, Color3.new(1,1,1), Enum.Font.GothamBlack)
LogoText.Size = UDim2.fromScale(1,1)
LogoText.TextXAlignment = Enum.TextXAlignment.Center

local Brand = Label(LogoFrame, "KOSMOS", 17, Settings.Text, Enum.Font.GothamBlack)
Brand.Position = UDim2.fromOffset(58, 9)
Brand.Size = UDim2.new(1,-60,0,25)

local BrandVersion = Label(LogoFrame, "UNRESTRICTED • V4", 8, Settings.Accent2, Enum.Font.GothamBold)
BrandVersion.Position = UDim2.fromOffset(58, 34)
BrandVersion.Size = UDim2.new(1,-60,0,18)

local Navigation = Create("ScrollingFrame", {
    Size = UDim2.new(1,-20,1,-105),
    Position = UDim2.fromOffset(10,90),
    BackgroundTransparency = 1,
    BorderSizePixel = 0,
    ScrollBarThickness = 2,
    ScrollBarImageColor3 = Settings.Accent
}, Sidebar)

local NavLayout = Create("UIListLayout", { Padding = UDim.new(0,5), SortOrder = Enum.SortOrder.LayoutOrder }, Navigation)
NavLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    Navigation.CanvasSize = UDim2.new(0, 0, 0, NavLayout.AbsoluteContentSize.Y + 10)
end)

-- Topbar & Search
local TopBar = Create("Frame", { Size = UDim2.new(1,-220,0,75), Position = UDim2.fromOffset(220,0), BackgroundColor3 = Settings.Background, BorderSizePixel = 0 }, Main)

local PageTitle = Label(TopBar, "Dashboard", 19, Settings.Text, Enum.Font.GothamBold)
PageTitle.Position = UDim2.fromOffset(25,10)
PageTitle.Size = UDim2.fromOffset(400,28)

local PageSub = Label(TopBar, "KOSMOS CONTROL CENTER", 9, Settings.SubText, Enum.Font.GothamMedium)
PageSub.Position = UDim2.fromOffset(25,38)
PageSub.Size = UDim2.fromOffset(400,18)

local Search = Create("TextBox", {
    Size = UDim2.fromOffset(260,35),
    Position = UDim2.new(1,-285,0,20),
    BackgroundColor3 = Settings.Element,
    BorderSizePixel = 0,
    PlaceholderText = "Search features...",
    PlaceholderColor3 = Settings.SubText,
    Text = "",
    TextColor3 = Settings.Text,
    TextSize = 10,
    Font = Enum.Font.GothamMedium,
    ClearTextOnFocus = false
}, TopBar)
Corner(Search, 7)
Stroke(Search, Settings.Border, 1)

local Content = Create("ScrollingFrame", {
    Size = UDim2.new(1,-245,1,-95),
    Position = UDim2.fromOffset(232,82),
    BackgroundTransparency = 1,
    BorderSizePixel = 0,
    ScrollBarThickness = 3,
    ScrollBarImageColor3 = Settings.Accent
}, Main)

local ContentLayout = Create("UIListLayout", { Padding = UDim.new(0,9), SortOrder = Enum.SortOrder.LayoutOrder }, Content)
ContentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    Content.CanvasSize = UDim2.new(0, 0, 0, ContentLayout.AbsoluteContentSize.Y + 20)
end)

--============================================================
-- INTERFACE CONTROL BUILDERS
--============================================================

local CurrentControls = {}
local SelectPage

local function ClearContent()
    for _, obj in ipairs(Content:GetChildren()) do
        if not obj:IsA("UIListLayout") then obj:Destroy() end
    end
    CurrentControls = {}
end

local function RegisterControl(object, name)
    table.insert(CurrentControls, { Object = object, Name = string.lower(name) })
end

local function Section(title, description)
    local Frame = Create("Frame", { Size = UDim2.new(1,-5,0,58), BackgroundColor3 = Settings.Panel, BorderSizePixel = 0 }, Content)
    Corner(Frame, 7)
    Stroke(Frame, Settings.Border, 1)
    
    local Title = Label(Frame, title, 12, Settings.Text, Enum.Font.GothamBold)
    Title.Position = UDim2.fromOffset(14,6)
    Title.Size = UDim2.new(1,-28,0,23)

    local Desc = Label(Frame, description or "", 9, Settings.SubText)
    Desc.Position = UDim2.fromOffset(14,29)
    Desc.Size = UDim2.new(1,-28,0,18)
    return Frame
end

local function Toggle(name, default, callback)
    Values[name] = default
    local Row = Create("TextButton", { Size = UDim2.new(1,-5,0,42), BackgroundColor3 = Settings.Element, BorderSizePixel = 0, Text = "", AutoButtonColor = false }, Content)
    Corner(Row, 6)

    local TextLabel = Label(Row, name, 10, Settings.Text)
    TextLabel.Position = UDim2.fromOffset(12,0)
    TextLabel.Size = UDim2.new(1,-75,1,0)

    local Switch = Create("Frame", { Size = UDim2.fromOffset(38,20), Position = UDim2.new(1,-52,0.5,-10), BackgroundColor3 = default and Settings.Accent or Color3.fromRGB(45,40,52), BorderSizePixel = 0 }, Row)
    Corner(Switch, 20)

    local Knob = Create("Frame", { Size = UDim2.fromOffset(14,14), Position = default and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7), BackgroundColor3 = Color3.new(1,1,1), BorderSizePixel = 0 }, Switch)
    Corner(Knob, 20)

    Row.MouseButton1Click:Connect(function()
        Values[name] = not Values[name]
        local enabled = Values[name]
        Tween(Switch, TweenInfo.new(0.18), { BackgroundColor3 = enabled and Settings.Accent or Color3.fromRGB(45,40,52) }):Play()
        Tween(Knob, TweenInfo.new(0.18, Enum.EasingStyle.Quint), { Position = enabled and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7) }):Play()
        if callback then callback(enabled) end
    end)
    RegisterControl(Row, name)
    return Row
end

local function Slider(name, min, max, default, suffix, callback)
    Values[name] = default
    local Row = Create("Frame", { Size = UDim2.new(1,-5,0,60), BackgroundColor3 = Settings.Element, BorderSizePixel = 0 }, Content)
    Corner(Row, 6)

    local Title = Label(Row, name, 10, Settings.Text)
    Title.Position = UDim2.fromOffset(12,5)
    Title.Size = UDim2.new(0.6,0,0,20)

    local Value = Label(Row, tostring(default)..suffix, 10, Settings.Accent2, Enum.Font.GothamBold)
    Value.Position = UDim2.new(0.6,0,0,5)
    Value.Size = UDim2.new(0.4,-12,0,20)
    Value.TextXAlignment = Enum.TextXAlignment.Right

    local Bar = Create("TextButton", { Size = UDim2.new(1,-24,0,5), Position = UDim2.fromOffset(12,40), BackgroundColor3 = Color3.fromRGB(45,40,53), BorderSizePixel = 0, Text = "" }, Row)
    Corner(Bar, 5)

    local Percentage = math.clamp((default-min)/(max-min), 0, 1)
    local Fill = Create("Frame", { Size = UDim2.new(Percentage,0,1,0), BackgroundColor3 = Settings.Accent, BorderSizePixel = 0 }, Bar)
    Corner(Fill, 5)

    local Dragging = false
    local function Update(input)
        local pos = math.clamp((input.Position.X-Bar.AbsolutePosition.X)/Bar.AbsoluteSize.X, 0, 1)
        local value = math.floor(min+(max-min)*pos)
        Values[name] = value
        Fill.Size = UDim2.new(pos,0,1,0)
        Value.Text = tostring(value)..suffix
        if callback then callback(value) end
    end

    Bar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then Dragging = true Update(input) end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then Dragging = false end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if Dragging and input.UserInputType == Enum.UserInputType.MouseMovement then Update(input) end
    end)
    RegisterControl(Row, name)
    return Row
end

local function Keybind(name, defaultKey, callback)
    Values[name] = defaultKey
    local Row = Create("Frame", { Size = UDim2.new(1,-5,0,45), BackgroundColor3 = Settings.Element, BorderSizePixel = 0 }, Content)
    Corner(Row, 6)

    local Title = Label(Row, name, 10, Settings.Text)
    Title.Position = UDim2.fromOffset(12,0)
    Title.Size = UDim2.new(0.6,0,1,0)

    local Button = Create("TextButton", {
        Size = UDim2.fromOffset(110,28),
        Position = UDim2.new(1,-122,0.5,-14),
        BackgroundColor3 = Settings.Panel,
        BorderSizePixel = 0,
        Text = defaultKey and defaultKey.Name or "NONE",
        TextColor3 = Settings.Accent2,
        TextSize = 9,
        Font = Enum.Font.GothamBold
    }, Row)
    Corner(Button, 5)
    Stroke(Button, Settings.Border, 1)

    local Listening = false
    Button.MouseButton1Click:Connect(function()
        Listening = true
        Button.Text = "PRESS KEY..."
    end)

    UserInputService.InputBegan:Connect(function(input)
        if Listening and input.UserInputType == Enum.UserInputType.Keyboard then
            Values[name] = input.KeyCode
            Button.Text = input.KeyCode.Name
            Listening = false
            if callback then callback(input.KeyCode) end
        end
    end)
    RegisterControl(Row, name)
    return Row
end

local function ActionButton(name, color, callback)
    local Button = Create("TextButton", {
        Size = UDim2.new(1,-5,0,40),
        BackgroundColor3 = color or Settings.Accent,
        BorderSizePixel = 0,
        Text = name,
        TextColor3 = Color3.new(1,1,1),
        TextSize = 10,
        Font = Enum.Font.GothamBold
    }, Content)
    Corner(Button, 6)
    Button.MouseButton1Click:Connect(callback)
    RegisterControl(Button, name)
    return Button
end

--============================================================
-- FULL CATEGORY RENDERERS
--============================================================

local Pages = {
    {"Dashboard", "rbxassetid://6031265976"},
    {"Combat", "rbxassetid://6034838634"},
    {"Visuals", "rbxassetid://6031763426"},
    {"Movement", "rbxassetid://6034508955"},
    {"Weapon", "rbxassetid://6035078863"},
    {"Player", "rbxassetid://6034287525"},
    {"Misc", "rbxassetid://6031280882"},
    {"Settings", "rbxassetid://6031280882"}
}

local CurrentPage = "Dashboard"

local function RenderDashboard()
    ClearContent()

    local ProfileCard = Create("Frame", { Size = UDim2.new(1,-5,0,85), BackgroundColor3 = Settings.Panel, BorderSizePixel = 0 }, Content)
    Corner(ProfileCard, 8)
    Stroke(ProfileCard, Settings.Border, 1)

    local AvatarImg = Create("ImageLabel", {
        Size = UDim2.fromOffset(65, 65),
        Position = UDim2.fromOffset(10, 10),
        BackgroundTransparency = 1,
        Image = "rbxthumb://type=AvatarHeadShot&id=" .. LocalPlayer.UserId .. "&w=150&h=150"
    }, ProfileCard)
    Corner(AvatarImg, 35)
    Stroke(AvatarImg, Settings.Accent, 1.5)

    local UserTitle = Label(ProfileCard, LocalPlayer.DisplayName .. " (@" .. LocalPlayer.Name .. ")", 13, Settings.Text, Enum.Font.GothamBold)
    UserTitle.Position = UDim2.fromOffset(85, 15)
    UserTitle.Size = UDim2.new(1, -95, 0, 22)

    local UserStatus = Label(ProfileCard, "STATUS: PREMIUM MASTER USER • KOSMOS V4 EXTENDED", 9, Settings.Accent2, Enum.Font.GothamBold)
    UserStatus.Position = UDim2.fromOffset(85, 38)
    UserStatus.Size = UDim2.new(1, -95, 0, 18)

    local UserID = Label(ProfileCard, "USER ID: " .. tostring(LocalPlayer.UserId), 8, Settings.SubText)
    UserID.Position = UDim2.fromOffset(85, 55)
    UserID.Size = UDim2.new(1, -95, 0, 15)

    Section("KOSMOS CONTROL CENTER", "Sessione attiva ed estesa a tutte le funzionalità")

    local Grid = Create("Frame", { Size = UDim2.new(1,-5,0,110), BackgroundTransparency = 1 }, Content)
    Create("UIGridLayout", { CellSize = UDim2.new(0.32,0,0,100), CellPadding = UDim2.new(0.02,0,0,0) }, Grid)

    local Cards = { {"FULL", "EXTENDED ENGINE"}, {"8", "MODULE CATEGORIES"}, {"V4", "UNRESTRICTED"} }
    for _, data in ipairs(Cards) do
        local Card = Create("Frame", { BackgroundColor3 = Settings.Element, BorderSizePixel = 0 }, Grid)
        Corner(Card, 8)
        Stroke(Card, Settings.Border, 1)
        
        local Num = Label(Card, data[1], 23, Settings.Accent, Enum.Font.GothamBlack)
        Num.Position = UDim2.fromOffset(14,12)
        Num.Size = UDim2.new(1,-28,0,32)

        local Lbl = Label(Card, data[2], 8, Settings.SubText, Enum.Font.GothamBold)
        Lbl.Position = UDim2.fromOffset(14,48)
        Lbl.Size = UDim2.new(1,-28,0,18)
    end
end

local function RenderCombat()
    ClearContent()
    Section("SILENT AIM & COMBAT ENGINE", "Impostazioni principali di mira silenziosa e ingaggio")
    Toggle("Silent Aim Active", Settings.Enabled, function(v) Settings.Enabled = v end)
    Toggle("Team Check (Ignora Alleati)", Settings.TeamCheck, function(v) Settings.TeamCheck = v end)
    Toggle("Wall Check (Verifica Visibilità)", Settings.WallCheck, function(v) Settings.WallCheck = v end)
    
    Section("ACCURACY & TARGETING PATTERN", "Sequenza di tiro e probabilità di impatto")
    Toggle("Hit Pattern Sequence (Head/Torso/Legs)", Settings.HitPatternEnabled, function(v) Settings.HitPatternEnabled = v end)
    Slider("Hit Probability", 1, 100, Settings.HitProbability, "%", function(v) Settings.HitProbability = v end)
    Slider("Target Base FOV", 10, 300, Settings.BaseFOV, " px", function(v) Settings.BaseFOV = v end)
    Toggle("Dynamic Target FOV Scaling", Settings.DynamicFOV, function(v) Settings.DynamicFOV = v end)
end

local function RenderVisuals()
    ClearContent()
    Section("HUD & OVERLAY INDICATORS", "Elementi visivi su schermo e mirino")
    Toggle("Show FOV Circle", Settings.ShowFOV, function(v) Settings.ShowFOV = v end)
    Toggle("Show Target Dot Indicator", Settings.ShowDot, function(v) Settings.ShowDot = v end)
    Toggle("Rainbow FOV Color", Settings.RainbowFOV, function(v) Settings.RainbowFOV = v end)
    Toggle("Custom Screen Crosshair", Settings.Crosshair, function(v) Settings.Crosshair = v end)
    
    Section("ESP PLAYER OVERLAYS", "Tracciamento completo dei giocatori")
    Toggle("Enable ESP Master Switch", Settings.ESPEnabled, function(v) Settings.ESPEnabled = v end)
    Toggle("ESP 2D Bounding Boxes", Settings.ESPBoxes, function(v) Settings.ESPBoxes = v end)
    Toggle("ESP Player Names", Settings.ESPNames, function(v) Settings.ESPNames = v end)
    Toggle("ESP Distance Indicator", Settings.ESPDistance, function(v) Settings.ESPDistance = v end)
    Toggle("ESP Health Bar", Settings.ESPHealth, function(v) Settings.ESPHealth = v end)
    Toggle("ESP Tracers (Snaplines)", Settings.ESPTracers, function(v) Settings.ESPTracers = v end)
    Toggle("ESP Player Chams (Highlight)", Settings.ESPChams, function(v) Settings.ESPChams = v end)

    Section("LIGHTING & WORLD MODIFIERS", "Modifiche all'ambiente di gioco")
    Toggle("Fullbright (Rimuovi Ombre e Buio)", Settings.Fullbright, function(v)
        Settings.Fullbright = v
        if not v then
            Lighting.Ambient = origAmbient
            Lighting.Brightness = origBrightness
            Lighting.GlobalShadows = origShadows
        end
    end)
end

local function RenderMovement()
    ClearContent()
    Section("SPEED & JUMP MODIFIERS", "Regolazione parametri di movimento")
    Toggle("Enable WalkSpeed Modifier", Settings.EnableWalkSpeed, function(v)
        Settings.EnableWalkSpeed = v
        if not v and LocalPlayer.Character then
            local humanoid = LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
            if humanoid then humanoid.WalkSpeed = 16 end
        end
    end)
    Slider("Custom WalkSpeed", 16, 250, Settings.WalkSpeed, " studs/s", function(v) Settings.WalkSpeed = v end)

    Toggle("Enable Jump Power Modifier", Settings.EnableJumpPower, function(v)
        Settings.EnableJumpPower = v
        if not v and LocalPlayer.Character then
            local humanoid = LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
            if humanoid then humanoid.JumpPower = 50 end
        end
    end)
    Slider("Custom JumpPower", 50, 350, Settings.JumpPower, " power", function(v) Settings.JumpPower = v end)

    Section("ADVANCED MOVEMENT MECHANICS", "Volo CFrame, collisioni ed abilità speciali")
    Toggle("Infinite Jump (Salto Infinito)", Settings.InfiniteJump, function(v) Settings.InfiniteJump = v end)
    Toggle("Noclip (Attraversa Muri)", Settings.Noclip, function(v) Settings.Noclip = v end)
    Toggle("Auto BHop (Bunny Hop)", Settings.BHop, function(v) Settings.BHop = v end)
    Toggle("CFrame Fly (Volo Fluido WASD)", Settings.FlyEnabled, function(v) Settings.FlyEnabled = v end)
    Slider("Fly Speed", 10, 200, Settings.FlySpeed, " speed", function(v) Settings.FlySpeed = v end)
end

local function RenderWeapon()
    ClearContent()
    Section("UNIVERSAL WEAPON MODIFIERS", "Modificatori universali per le armi da fuoco")
    Toggle("No Recoil (Azzera Rinculo)", Settings.NoRecoil, function(v) Settings.NoRecoil = v end)
    Toggle("No Spread (Azzera Dispersione)", Settings.NoSpread, function(v) Settings.NoSpread = v end)
    Toggle("Rapid Fire (Cadenza di sparo massima)", Settings.RapidFire, function(v) Settings.RapidFire = v end)
    Toggle("Always Auto (Trasforma in Automatico)", Settings.AlwaysAuto, function(v) Settings.AlwaysAuto = v end)
end

local function RenderPlayer()
    ClearContent()
    Section("PLAYER & ANTI-AIM MECHANICS", "Rotazione desincronizzata e modificatori personaggio")
    Toggle("Spinbot (Anti-Aim Rotation)", Settings.Spinbot, function(v) Settings.Spinbot = v end)
    Slider("Spinbot Speed", 5, 100, Settings.SpinSpeed, " deg", function(v) Settings.SpinSpeed = v end)
end

local function RenderMisc()
    ClearContent()
    Section("SERVER & SESSION UTILITIES", "Gestione del server e riconnessione rapida")
    ActionButton("SERVER HOP (Cambia Server)", Settings.Accent, function()
        local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")).data
        for _, s in ipairs(servers) do
            if s.playing ~= s.maxPlayers and s.id ~= game.JobId then
                TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, LocalPlayer)
                break
            end
        end
    end)

    ActionButton("REJOIN SERVER (Riconnetti)", Settings.Panel, function()
        TeleportService:Teleport(game.PlaceId, LocalPlayer)
    end)

    Section("AUTOMATION & PERFORMANCE", "Prevenzione disconnessione e prestazioni")
    Toggle("Anti-AFK Automagico", Settings.AntiAFK, function(v) Settings.AntiAFK = v end)
end

local function RenderSettings()
    ClearContent()

    local StatusLbl = Label(Content, "Ready.", 9, Settings.SubText)
    StatusLbl.Size = UDim2.new(1, -5, 0, 18)

    Section("1. SAVE NEW CONFIGURATION", "Crea una nuova configurazione salvata")

    local SaveRow = Create("Frame", { Size = UDim2.new(1,-5,0,45), BackgroundColor3 = Settings.Element, BorderSizePixel = 0 }, Content)
    Corner(SaveRow, 6)
    
    local NameLabel = Label(SaveRow, "Nome Config:", 10, Settings.Text)
    NameLabel.Position = UDim2.fromOffset(12, 0)
    NameLabel.Size = UDim2.new(0.4, 0, 1, 0)

    local SaveNameBox = Create("TextBox", {
        Size = UDim2.fromOffset(180, 28),
        Position = UDim2.new(1, -192, 0.5, -14),
        BackgroundColor3 = Settings.Panel,
        Text = newConfigInputName,
        PlaceholderText = "Nome config...",
        TextColor3 = Settings.Accent2,
        TextSize = 10,
        Font = Enum.Font.GothamBold
    }, SaveRow)
    Corner(SaveNameBox, 5)
    Stroke(SaveNameBox, Settings.Border, 1)

    SaveNameBox:GetPropertyChangedSignal("Text"):Connect(function()
        newConfigInputName = SaveNameBox.Text
    end)

    ActionButton("SALVA NUOVA CONFIGURAZIONE", Settings.Accent, function()
        if newConfigInputName == "" then
            StatusLbl.Text = "Errore: Nome non valido!"
            StatusLbl.TextColor3 = Settings.Red
            return
        end

        if SaveConfig(newConfigInputName) then
            StatusLbl.Text = "Configurazione '" .. newConfigInputName .. "' salvata!"
            StatusLbl.TextColor3 = Settings.Green
            SelectPage("Settings")
        end
    end)

    Section("2. MANAGE CONFIGURATIONS", "Seleziona, carica o elimina configurazioni")

    local savedList = GetSavedConfigs()
    if #savedList > 0 and selectedDropdownConfig == "" then
        selectedDropdownConfig = savedList[1]
    end

    local DropdownContainer = Create("Frame", { Size = UDim2.new(1,-5,0,45), BackgroundColor3 = Settings.Element, BorderSizePixel = 0 }, Content)
    Corner(DropdownContainer, 6)

    local DropTitle = Label(DropdownContainer, "Config Selezionata:", 10, Settings.Text)
    DropTitle.Position = UDim2.fromOffset(12, 0)
    DropTitle.Size = UDim2.new(0.4, 0, 1, 0)

    local DropBtn = Create("TextButton", {
        Size = UDim2.fromOffset(180, 28),
        Position = UDim2.new(1, -192, 0.5, -14),
        BackgroundColor3 = Settings.Panel,
        Text = selectedDropdownConfig ~= "" and (selectedDropdownConfig .. "  ▼") or "Nessuna Config  ▼",
        TextColor3 = Settings.Accent2,
        TextSize = 10,
        Font = Enum.Font.GothamBold
    }, DropdownContainer)
    Corner(DropBtn, 5)
    Stroke(DropBtn, Settings.Border, 1)

    ActionButton("LOAD CONFIG", Settings.Panel, function()
        if selectedDropdownConfig ~= "" and LoadConfig(selectedDropdownConfig) then
            StatusLbl.Text = "Configurata '" .. selectedDropdownConfig .. "' caricata!"
            StatusLbl.TextColor3 = Settings.Green
            SelectPage("Settings")
        end
    end)

    ActionButton("SET AUTOLOAD", Settings.Element, function()
        if selectedDropdownConfig ~= "" then
            SetAutoLoadConfig(selectedDropdownConfig)
            StatusLbl.Text = "AutoLoad impostato su '" .. selectedDropdownConfig .. "'!"
            StatusLbl.TextColor3 = Settings.Green
            SelectPage("Settings")
        end
    end)

    ActionButton("DELETE CONFIG", Settings.Red, function()
        if selectedDropdownConfig ~= "" and DeleteConfig(selectedDropdownConfig) then
            StatusLbl.Text = "Configurazione eliminata!"
            StatusLbl.TextColor3 = Settings.Green
            selectedDropdownConfig = ""
            SelectPage("Settings")
        end
    end)

    Section("KEYBINDS & UNLOAD", "Scelte rapide ed esecuzione dello script")
    Keybind("Menu Toggle Key", Settings.ToggleKey, function(key) Settings.ToggleKey = key end)
    Keybind("Unload Script Key", Settings.UnloadKey, function(key) Settings.UnloadKey = key end)
    ActionButton("UNLOAD SCRIPT", Settings.Red, UnloadScript)
end

SelectPage = function(name)
    CurrentPage = name
    for _, button in ipairs(Navigation:GetChildren()) do
        if button:IsA("TextButton") then
            local page = button:GetAttribute("Page")
            local isSel = (page == name)
            button.BackgroundColor3 = isSel and Settings.Accent or Settings.Sidebar
            local icon = button:FindFirstChild("IconImage")
            local text = button:FindFirstChild("Text")
            if icon then icon.ImageColor3 = isSel and Color3.new(1,1,1) or Settings.SubText end
            if text then text.TextColor3 = isSel and Color3.new(1,1,1) or Settings.SubText end
        end
    end

    PageTitle.Text = name
    PageSub.Text = "KOSMOS / " .. string.upper(name)

    if name == "Dashboard" then RenderDashboard()
    elseif name == "Combat" then RenderCombat()
    elseif name == "Visuals" then RenderVisuals()
    elseif name == "Movement" then RenderMovement()
    elseif name == "Weapon" then RenderWeapon()
    elseif name == "Player" then RenderPlayer()
    elseif name == "Misc" then RenderMisc()
    elseif name == "Settings" then RenderSettings()
    end
end

-- Navigation Buttons Creation
for index, data in ipairs(Pages) do
    local name, iconAsset = data[1], data[2]
    local Button = Create("TextButton", {
        Size = UDim2.new(1,0,0,39),
        BackgroundColor3 = Settings.Sidebar,
        BorderSizePixel = 0,
        Text = "",
        AutoButtonColor = false,
        LayoutOrder = index
    }, Navigation)

    Button:SetAttribute("Page", name)
    Corner(Button, 6)

    local Icon = Create("ImageLabel", {
        Name = "IconImage",
        Size = UDim2.fromOffset(18, 18),
        Position = UDim2.fromOffset(14, 10),
        BackgroundTransparency = 1,
        Image = iconAsset,
        ImageColor3 = Settings.SubText
    }, Button)

    local TextLabel = Label(Button, name, 10, Settings.SubText, Enum.Font.GothamMedium)
    TextLabel.Name = "Text"
    TextLabel.Position = UDim2.fromOffset(45,0)
    TextLabel.Size = UDim2.new(1,-55,1,0)

    Button.MouseButton1Click:Connect(function() SelectPage(name) end)
end

-- Search Engine Filtering
Search:GetPropertyChangedSignal("Text"):Connect(function()
    local query = string.lower(Search.Text)
    for _, control in ipairs(CurrentControls) do
        if query == "" then
            control.Object.Visible = true
        else
            control.Object.Visible = string.find(control.Name, query, 1, true) ~= nil
        end
    end
end)

-- Keybind Listeners
inputConnection = UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Settings.ToggleKey then
        Main.Visible = not Main.Visible
    elseif input.KeyCode == Settings.UnloadKey then
        UnloadScript()
    end
end)

--============================================================
-- STARTUP INITIALIZATION
--============================================================

ShowLoadingScreen(function()
    local autoConfig = GetAutoLoadConfig()
    if autoConfig then LoadConfig(autoConfig) end
    Main.Visible = true
    SelectPage("Dashboard")
end)
